/* exported getKeys */
// define a new function: getKeys with 1 parameter: object
// assign an empty array to a new variable: result
// use a for in loop to loop through the object
// in the code block, push each key into the result array
// return the result array
function getKeys(object) {
  var result = [];
  for (var key in object) {
    result.push(key);
  } return result;
}
// define a new function: getKeys with 1 parameter: object
// assign an empty array to a new variable: result
// use a for in loop to loop through the object
// in the code block, push each key into the result array
// return the result array
