/* exported getValue */
// define a new function: getValue with 2 parameters: object and key
// use a for in loop to get each key in the object
// return the object at index key
function getValue(object, key) {
  return object[key];
}
// define a new function: getValue with 2 parameters: object and key
// return the value of the key property of the object
