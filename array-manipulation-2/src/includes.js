/* exported includes */
// define a new function: includes with 2 parameters: array and value
// use a for loop to loop through the array as many times as the length of the array
// in the code block, use an if statement with the condition array[i] === value
// if true, return true
// if false, return false
function includes(array, value) {
  for (var i = 0; i < array.length; i++) {
    if (array[i] === value) {
      return true;
    }
  } return false;
}
// define a new function: includes with 2 parameters: array and value
// define a new function: includes with 2 parameters: array and value
// in the code block, use an if statement with the condition that the item at index i of the array
// is strictly equal to the value argument
// if true, return true
// if false, return false
