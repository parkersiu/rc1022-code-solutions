/* exported isUpperCased */
// define a new function named isUpperCased with 1 parameter: word
// convert the string to an array
// use a for loop to loop through the array that breaks after reaching the end of the array
// in the code block use an if statement to compare each item at index i to the uppercase method of the array
// if true, return true and if false return false

function isUpperCased(word) {
  return word === word.toUpperCase();
}

// define a new function named isUpperCased with 1 parameter: word
// Use a comparison operator to determine if word is strictly equal to the uppercase method
// of the word object and return a boolean value
